const info = require("./others/info");
const servers = require("./others/servers");
const paths = require("./paths");
const components = require("./components");

module.exports = {
  openapi: "3.0.0",
  info,
  servers,
  paths,
  components
};
